shamefully-hoist=false
strict-peer-dependencies=false

# Resolve platform-specific packages for all targets so the lockfile
# is stable across macOS, Windows, and Linux
supportedArchitectures.os=current,win32,darwin,linux
supportedArchitectures.cpu=x64,arm64
